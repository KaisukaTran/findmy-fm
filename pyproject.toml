[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "findmy"
version = "0.1.0"
description = "FINDMY trading bot"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
