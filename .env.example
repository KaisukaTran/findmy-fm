# FINDMY Trading Bot - Environment Configuration Example
# Copy this file to .env and fill in real values for local development
# DO NOT commit .env to version control - it will be ignored by .gitignore

# ===== Broker / Live Trading Credentials (for future adapters v2.0+) =====
# These are optional for now - required when live trading support is added
BROKER_API_KEY=your_broker_api_key_here
BROKER_API_SECRET=your_broker_api_secret_here
BROKER_BASE_URL=https://api.broker.com

# ===== Demo Fund Configuration (Optional) =====
# Amount of USD available for KSS pyramid demo sessions
# Default: 10000 (10,000 USD)
DEMO_ISOLATED_FUND=10000

# ===== Application Secrets (REQUIRED) =====
# Used for JWT signing, session encryption, and other security features
# In production, use a strong random string (min 32 characters)
# Example: openssl rand -hex 32
APP_SECRET_KEY=change_this_to_a_strong_random_string_in_production

# ===== Database Configuration (Optional) =====
# If not set, defaults to SQLite at data/findmy.db
# For PostgreSQL: postgresql://user:password@localhost:5432/findmy
# For MySQL: mysql://user:password@localhost:3306/findmy
# DATABASE_URL=sqlite:///data/findmy.db
